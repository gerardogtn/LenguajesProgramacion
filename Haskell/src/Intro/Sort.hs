module Intro.Sort
    ( insertion,
      quick,
      merge
    ) where

insertion :: (Ord a) => [a] -> [a]
insertion [] = []
insertion list = list -- STUB

quick :: (Ord a) => [a] -> [a]
quick [] = []
quick list = list -- STUB

merge :: (Ord a) => [a] -> [a]
merge [] = []
merge list = list -- STUB
